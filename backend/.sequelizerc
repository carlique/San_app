var config = require ('config');
//var path = require('path');

//config.database.config = path.resolve('config', 'database.json');

config.database.config = __filename;

module.exports = config.database;
